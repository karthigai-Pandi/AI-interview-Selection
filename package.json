{
  "name": "ai-interview-selection",
  "private": true,
  "version": "0.1.0",
  "description": "AI Interview & Hiring Platform built with MERN, modern SaaS UI, and AI-powered recruitment workflows.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "concurrently \"npm --workspace client run dev\" \"npm --workspace server run dev\"",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "start": "npm --workspace server run start",
    "lint": "npm --workspace client run lint && npm --workspace server run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
